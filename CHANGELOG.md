# Changelog

本项目的重要版本变化记录在此文件中。

## [未发布]

### Windows Docker 一键启动

- Windows 一键命令现在会在缺少 Docker 时通过中国内地加速地址下载 Docker Desktop，校验 Docker Inc. 数字签名，安装 WSL 2 后端并等待引擎启动。
- Docker Desktop、GHCR、Docker Hub、GitHub 文件、Node.js、npm、PyPI、Python 运行时和 Debian 依赖默认切换到可用的中国内地源；不会配置已经关闭的中科大 Docker Hub 缓存。
- 公开发布 `ghcr.io/honghudavy-star/auto-voucher:latest`，Windows 用户可以通过一条 PowerShell 命令直接拉取并启动。
- 新增 PowerShell Docker 安装器：客户端无需 Git、Node.js 或 Python，只拉取由 `win-office` 预先构建的镜像；更新启动失败时按旧镜像 ID 自动回退，且不会删除 `auto-voucher-data` 数据卷。
- 新增 Docker 多阶段镜像、Compose 编排和 Windows 双击启动脚本，端口仅发布到宿主机 `127.0.0.1`。
- SQLite、归档和诊断数据保存在 `auto-voucher-data` 命名卷，重建容器不会删除业务数据。
- 镜像包含 OCR/PDF 组件，以非 root 用户运行，并配置健康检查、自动重启、日志轮转和资源上限。
- 新增 Linux AMD64 构建、Compose 冒烟和 Trivy Critical 漏洞扫描 CI 门禁。
- 新增 Windows Docker 一键启动指南，说明首次启动、更新、停止、日志、端口修改、数据保留和故障排查。
- Windows 用户统一使用 Docker Desktop；需要保存连接器密钥的 ERP/OA API 直连仍需按目标环境单独验证。

### 审批与银行联合处理

- 审批数据处理页按审批编号展示银行流水与审批记录的全外连接，保留银行独有、审批独有、金额差异和匹配成功四种状态。
- 审批原始数据保持只读；新增的逐条“科目”字段只能从有效科目主数据中选择。
- 凭证场景支持将借方科目配置为“审批数据处理 · 科目”，贷方继续使用固定银行科目。
- 仅审批编号匹配且金额一致的记录可批量自动生成草稿；其余记录由用户选择凭证场景后手动生成，不写入异常清单。

### 金蝶连接

- 将金蝶云星空认证从账号密码 `ValidateUser` 切换为与 finweb 一致的
  `AcctID + Username + AppID + AppSecret` 官方 SDK 签名认证。
- 连接测试改为真实查询 `BD_AccountBook`，同时验证签名、网络和基础查询权限。
- 金蝶 AppSecret 只保存到操作系统密钥库，旧密码配置不再读取；旧连接状态会失效并要求重新配置、测试。
- 凭证草稿保存采用 finweb 的账簿、凭证字、币种和幂等标记参数，仍不提交、审核或过账。
- 新增金蝶查询、报表、推送和幂等回查参数文档。
- 基础资料同步扩展到客户、供应商、部门、员工、项目、辅助资料、核算维度、币种等已审核表单，并记录每一类的成功、空结果或失败状态。
- “目标主数据”改为按实际数据动态分类的单表视图，支持来源、状态、关键词筛选和分页。
- “系统与数据”移除 ERP 模板路径卡片；“凭证规则”更名为“凭证场景”；移除“测试上线”一级模块。

### 验证边界

- 官方 SDK 初始化、模拟查询/保存合同、完整后端测试和前端构建已验证。
- 已使用当前金蝶测试账套执行只读连接探测和基础资料逐类查询；尚未保存凭证草稿，写入验收仍需明确执行。

## [0.2.2] - 2026-07-26

面向首次配置和日常操作的界面可用性更新。

### 改进

- 重做接入方案表单布局，统一输入框、下拉框、分组间距和焦点状态。
- 将凭证工作台内部步骤改为“导入资料、业务事项、生成与复核、推送状态、查询审计”，并增加简短说明。
- 将连接器、OCR、通用设置、诊断日志和备份恢复归入独立一级入口“系统工具”。
- 凭证工作台与系统工具支持再次点击收起，两个入口互斥展开。
- 修复较慢网络下载核心包时继承 30 秒清单超时的问题；失败后保留断点并进入可重试错误状态。

### 验证边界

- 本版本不改变凭证生成、审批、推送、连接器或数据库契约。
- ERP 真实连接能力仍须在客户提供的产品版本、接口授权和测试账套中验收。
- Windows Docker 安装器通过 Docker Inc. Authenticode 签名校验后才执行。

## [0.2.0] - 2026-07-26

首个面向公开体验的 Windows 版本。

### 新增

- 五入口生产工作台：接入方案、系统与数据、凭证规则、测试上线、凭证工作台。
- 金蝶云星空、用友 U8 V12+、浪潮海岳 GS Cloud 目标适配器框架。
- 飞书审批、CSV、XLSX、XML、XBRL、OFD、PDF、图片和银行流水来源。
- Windows Docker 启动、镜像更新和失败回退。
- 一键复制脱敏问题摘要与诊断包。
- ERP API 与文件模板两类确定性接入方案。

### 安全边界

- 删除演示公司、演示账簿、默认规则、默认审批通过和固定置信度。
- 生产启用前必须通过方案、系统、规则、环境和测试账套门槛。
- 所有连接器只保存凭证草稿，不自动提交、审核、过账或结账。
- 连接器密钥只进入操作系统密钥库。
- Windows Docker 端通过镜像摘要、健康检查和数据卷保留保障更新安全。

### 已知边界

- ERP 适配器已通过模拟合同测试；真实 API 能力必须在客户提供的产品版本、接口授权和测试账套中单独验收。
- OCR/PDF 是按需组件，识别结果仅作为候选数据，关键字段需要人工确认。

[0.2.2]: https://github.com/honghudavy-star/auto-voucher/releases/tag/v0.2.2
[0.2.0]: https://github.com/honghudavy-star/auto-voucher/releases/tag/v0.2.0
